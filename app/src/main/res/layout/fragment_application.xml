<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="5dp"
    android:orientation="horizontal"
    android:layout_marginBottom="10dp">

    <ImageView
        android:id="@+id/app_icon"
        android:layout_width="@dimen/app_icon_size"
        android:layout_height="@dimen/app_icon_size"
        android:layout_centerVertical="true"
        tools:src="@mipmap/ic_launcher"/>

    <TextView
        android:layout_toEndOf="@id/app_icon"
        android:id="@+id/item_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/text_margin"
        android:textAppearance="?attr/textAppearanceListItem"
        android:textColor="@color/defaultTextColor"
        tools:text="Lepro Meter"/>
    <ImageView
        android:layout_centerVertical="true"
        android:layout_marginEnd="10dp"
        android:layout_toStartOf="@id/lazy_app"
        android:id="@+id/productive_app"
        android:layout_width="@dimen/app_type_icon_size"
        android:layout_height="@dimen/app_type_icon_size"
        android:padding="5dp"
        android:src="@drawable/ic_star"
        />

    <ImageView
        android:layout_centerVertical="true"
        android:layout_marginEnd="10dp"
        android:layout_alignParentEnd="true"
        android:id="@+id/lazy_app"
        android:layout_width="@dimen/app_type_icon_size"
        android:layout_height="@dimen/app_type_icon_size"
        android:src="@drawable/ic_sloth"
        android:padding="5dp"
        tools:background="@color/colorSelectedHighlight"/>
</RelativeLayout>
